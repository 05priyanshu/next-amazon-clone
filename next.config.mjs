/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images-eu.ssl-images-amazon.com",
      "fakestoreapi.com",
      "pngimg.com",
    ],
  },
};

export default nextConfig;
